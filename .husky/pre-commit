#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

# Check if there are any staged files after lint-staged
if git diff --cached --quiet; then
  echo "⚠️  No files staged after lint-staged. Commit cancelled."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
