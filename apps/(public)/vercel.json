{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "bun install && cd packages/database && bun x prisma generate",
  "buildCommand": "bun run build:public",
  "outputDirectory": "apps/(public)/.next"
}
